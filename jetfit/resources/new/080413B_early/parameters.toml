# MCMC Parameter Configuriation File

# -----------------------------
# ------- FREE PARAMS ---------
#------------------------------
[E]
    scale = "log"

    [E.prior]
        type  = "uniform"
        lower = -2.0
        upper = 3.0

[rho0]
    scale = "log"

    [rho0.prior]
        type  = "uniform"
        lower = -6.0
        upper = 9.0

[eps_e]
    scale = "log"

    [eps_e.prior]
        type  = "uniform"
        lower = -4.0
        upper = 0.0

[eps_b]
    scale = "log"

    [eps_b.prior]
        type  = "uniform"
        lower = -6.0
        upper = 0.0

[p]
    scale = "linear"

    [p.prior]
        type  = "uniform"
        lower = 1.5
        upper = 3.5
        initial_guess = 2.25
        initial_sigma = 0.25

[k]
    scale = "linear"

    [k.prior]
        type  = "uniform"
        lower = -1.0
        upper = 3.0

[ebv_sf]
    scale = "linear"

    [ebv_sf.prior]
        type  = "uniform"
        lower = 0.0
        upper = 1.0

# -----------------------------
# ------- FIXED PARAMS --------
#------------------------------
[z]
    group = "observational"
    scale = "linear"
    value = 1.1

[dL]
    group = "observational"
    scale = "linear"
    value = 2.36

[X]
    group = "observational"
    scale = "linear"
    value = 0.7

[ebv_mw]
    scale = "linear"
    value = 0.0315

# -----------------------------
# ---- CALIBRATION OFFSETS ----
#------------------------------
[g_offset]
    scale = "linear"

    [g_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[r_offset]
    scale = "linear"

    [r_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[i_offset]
    scale = "linear"

    [i_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[z_offset]
    scale = "linear"

    [z_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[J_offset]
    scale = "linear"

    [J_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[H_offset]
    scale = "linear"

    [H_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[K_offset]
    scale = "linear"

    [K_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[slop]
    scale = "linear"

    [slop.prior]
        type  = "uniform"
        lower = 0.0
        upper = 0.1