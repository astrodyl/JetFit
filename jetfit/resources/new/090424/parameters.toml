# MCMC Parameter Configuriation File

# -----------------------------
# ------- FREE PARAMS ---------
#------------------------------
[E]
    scale = "log"

    [E.prior]
        type  = "uniform"
        lower = -2.0
        upper = 3.0

[rho0]
    scale = "log"

    [rho0.prior]
        type  = "uniform"
        lower = -6.0
        upper = 9.0

[eps_e]
    scale = "log"

    [eps_e.prior]
        type  = "uniform"
        lower = -4.0
        upper = 0.0

[eps_b]
    scale = "log"

    [eps_b.prior]
        type  = "uniform"
        lower = -6.0
        upper = 0.0

[p]
    scale = "linear"

    [p.prior]
        type  = "uniform"
        lower = 1.5
        upper = 3.5
        initial_guess=2.5
        initial_sigma=0.5

[k]
    scale = "linear"

    [k.prior]
        type  = "uniform"
        lower = -1.0
        upper = 3.0
        initial_guess=1.0
        initial_sigma=1.0

[ebv_sf]
    scale = "linear"

    [ebv_sf.prior]
        type  = "uniform"
        lower = 0.0
        upper = 1.0

# -----------------------------
# ------- FIXED PARAMS --------
#------------------------------
[z]
    scale = "linear"
    value = 0.544

[dL]
    scale = "linear"
    value = 1.00

[X]
    scale = "linear"
    value = 0.7

[ebv_mw]
    scale = "linear"
    value = 0.022

# -----------------------------
# ---- CALIBRATION OFFSETS ----
#------------------------------
[uvw2_offset]
    scale = "linear"

    [uvw2_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.04

[uvm2_offset]
    scale = "linear"

    [uvm2_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[uvw1_offset]
    scale = "linear"

    [uvw1_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.04

[uvot-u_offset]
    scale = "linear"

    [uvot-u_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[uvot-b_offset]
    scale = "linear"

    [uvot-b_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.02

[uvot-v_offset]
    scale = "linear"

    [uvot-v_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.02

[r_offset]
    scale = "linear"

    [r_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[i_offset]
    scale = "linear"

    [i_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[J_offset]
    scale = "linear"

    [J_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.04

[H_offset]
    scale = "linear"

    [H_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.05

# -----------------------------
# ------- HOST GALAXY ---------
# -----------------------------
[uvw2_host]
    scale = "log"

    [uvw2_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[uvm2_host]
    scale = "log"

    [uvm2_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[uvw1_host]
    scale = "log"

    [uvw1_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[uvot-u_host]
    scale = "log"

    [uvot-u_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[uvot-b_host]
    scale = "log"

    [uvot-b_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[uvot-v_host]
    scale = "log"

    [uvot-v_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[r_host]
    scale = "log"

    [r_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

[i_host]
    scale = "log"

    [i_host.prior]
        type  = "uniform"
        lower = -3.0
        upper = 0.0

# -----------------------------
# ----------- SLOP ------------
# -----------------------------
[slop]
    scale = "linear"

    [slop.prior]
        type  = "uniform"
        lower = 0.0
        upper = 0.3
