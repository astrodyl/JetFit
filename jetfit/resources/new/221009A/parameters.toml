# MCMC Parameter Configuriation File

# -----------------------------
# ------- FREE PARAMS ---------
#------------------------------
[E]
    scale = "log"

    [E.prior]
        type  = "uniform"
        lower = -2.0
        upper = 3.0

[rho0]
    scale = "log"

    [rho0.prior]
        type  = "uniform"
        lower = -6.0
        upper = 9.0

[eps_e]
    scale = "log"

    [eps_e.prior]
        type  = "uniform"
        lower = -4.0
        upper = 0.0

[eps_b]
    scale = "log"

    [eps_b.prior]
        type  = "uniform"
        lower = -6.0
        upper = 0.0

[p]
    scale = "linear"

    [p.prior]
        type  = "uniform"
        lower = 1.5
        upper = 3.5
        initial_guess=2.5
        initial_sigma=0.5

[k]
    scale = "linear"

    [k.prior]
        type  = "uniform"
        lower = -1.0
        upper = 3.0
        initial_guess=1.0
        initial_sigma=1.0

[ebv_sf]
    scale = "linear"

    [ebv_sf.prior]
        type  = "uniform"
        lower = 0.0
        upper = 1.0

# -----------------------------
# ------- FIXED PARAMS --------
#------------------------------
[z]
    group = "observational"
    scale = "linear"
    value = 0.151

[dL]
    group = "observational"
    scale = "linear"
    value = 0.229

[X]
    group = "observational"
    scale = "linear"
    value = 0.7

[ebv_mw]
    scale = "linear"
    value = 1.3021

# -----------------------------
# ---- CALIBRATION OFFSETS ----
#------------------------------
[g1_offset]
    scale = "linear"

    [g1_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[r1_offset]
    scale = "linear"

    [r1_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[i1_offset]
    scale = "linear"

    [i1_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[z1_offset]
    scale = "linear"

    [z1_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.04

[g2_offset]
    scale = "linear"

    [g2_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[r2_offset]
    scale = "linear"

    [r2_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[i2_offset]
    scale = "linear"

    [i2_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.03

[z2_offset]
    scale = "linear"

    [z2_offset.prior]
        type  = "gaussian"
        mu = 0.0
        sigma = 0.04

[slop]
    scale = "linear"

    [slop.prior]
        type  = "uniform"
        lower = 0.0
        upper = 0.3
